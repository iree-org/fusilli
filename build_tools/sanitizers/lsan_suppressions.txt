# LSAN suppressions for leaks in external libraries (IREE, ROCm)

# TODO(iree-org/iree#23561): RCCL library leaks during dynamic symbol initialization.
leak:iree_hal_hip_nccl_dynamic_symbols_initialize

# Suppressions from IREE project
leak:libamdhip64.so
leak:libhsa-runtime64.so
leak:libamd_comgr.so

# ROCm libraries loaded via dlopen may appear as <unknown module> in LSAN
# stack traces when the library is dlclose'd before LSAN's exit-time check.
# This suppresses leaks from any unresolvable module.
leak:<unknown module>
